# SeCAT v5.0.2 — parameter template
# Copy this file, replace every /path/to/... entry, and pass it with -params-file.
# The statistical defaults below are those used in the SeCAT paper.

manifest: /path/to/secat_manifest.tsv
reference_db: /path/to/SILVA_138.2_SSURef_NR99_tax_silva_full_align_trunc.fasta
outdir: /path/to/secat_output
# comma-separated host paths bound into the container (REQUIRED for singularity)
bind_paths: /path/to/data,/path/to/scratch

selection_mode: roster
selection_file: selection_roster.txt

min_sample_depth: 10000
min_asv_prevalence: 2
min_asv_reads: 50

use_all_asvs: true
mapping_support_band: 250
mapping_support_warn: 0.70
mapping_support_fail: 0.40
exclude_unstable_from_consensus: true

min_overlap: 50
min_consensus_studies: 3
min_consensus_coverage: 0.50
verdict_consensus_tolerance_bp: 500

sim_structure_matched_null: true
sim_cluster_identity: 0.97
sim_max_template: 1500
sim_add_errors: true
sim_error_rate: 0.003
sim_add_chimeras: false

null_model_p_threshold: 0.05
null_model_min_consecutive: 2
null_model_min_trim_bp: 5
distance_cutoff_threshold: 0.20
changepoint_penalty_method: MANUAL
changepoint_scan_min: 0.4
changepoint_scan_max: 2.4
changepoint_scan_by: 0.1
changepoint_bootstrap_n: 50
changepoint_seed: 10010

bc_ceiling_threshold: 0.20
retention_floor_threshold: 0.70

validation_levels: ASV,Genus,Family

keep_intermediates: false
